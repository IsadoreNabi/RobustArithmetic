% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/safeguards.R
\name{ra_fast_level_status}
\alias{ra_fast_level_status}
\title{Status of the fast level in the current session}
\usage{
ra_fast_level_status()
}
\value{
A list with four fields: \code{degraded}, a logical scalar; \code{reason}, an
empty character vector while healthy or a character scalar naming every
active cause; \code{sentinels}, the data frame returned by
\code{\link[=ra_check_sentinels]{ra_check_sentinels()}} at load time or \code{NULL} if that verification could
not run; and \code{audit}, the first-use comparison against MPFR or \code{NULL} until
that audit runs.
}
\description{
Report whether the fast level is degraded, why it is degraded, and the two
verifications that determine its session state.
}
\details{
The status is read-only. A failed load-time sentinel verification
degrades the level immediately. With \code{Rmpfr} available, the first fast
evaluation also runs \code{\link[=ra_measure_library_error]{ra_measure_library_error()}} and degrades the level
if an observed error exceeds its declared slack. The first result affected
by a degraded state emits one warning of class \code{ra_fast_level_unsafe} and
escalates to the rigorous level; later results remain rigorous without
repeating the warning. Without \code{Rmpfr}, every affected evaluation refuses
with an error of the same class because no rigorous result can replace it.
}
\section{Methodological notes}{

Session state and result provenance answer different questions. This
function records whether the fast route is available and why. \code{\link[=ra_prov]{ra_prov()}}
records the guarantee carried by one returned interval, so an interval
escalated after degradation carries \code{theorem} without changing its class.
}

\section{Dependencies}{

Base R. The first-use audit requires \code{Rmpfr}; reading the status does not.
}

\examples{
status <- ra_fast_level_status()
status$degraded
all(status$sentinels$ok)

}
\references{
Fousse, L., Hanrot, G., Lefevre, V., Pelissier, P., & Zimmermann, P. (2007).
MPFR: A multiple-precision binary floating-point library with correct
rounding. \emph{ACM Transactions on Mathematical Software, 33}(2), Article 13.
https://doi.org/10.1145/1236463.1236468
}
\seealso{
\code{\link[=ra_check_sentinels]{ra_check_sentinels()}}, \code{\link[=ra_measure_library_error]{ra_measure_library_error()}} and
\code{\link[=ra_prov]{ra_prov()}}.
}
