% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elementary.R
\name{ra_escalation_show}
\alias{ra_escalation_show}
\alias{format.ra_escalation}
\alias{print.ra_escalation}
\alias{as.data.frame.ra_escalation}
\alias{summary.ra_escalation}
\alias{print.ra_escalation_summary}
\title{Show the result of an escalation}
\usage{
\method{format}{ra_escalation}(x, ...)

\method{print}{ra_escalation}(x, ...)

\method{as.data.frame}{ra_escalation}(x, row.names = NULL, optional = FALSE, ...)

\method{summary}{ra_escalation}(object, ...)

\method{print}{ra_escalation_summary}(x, ...)
}
\arguments{
\item{x}{An object of class \code{ra_escalation}.}

\item{...}{Further arguments, ignored, present for consistency with the
generics.}

\item{row.names, optional}{Arguments of the \code{as.data.frame} generic. The
first is used; the second is ignored.}

\item{object}{An object of class \code{ra_escalation}, for \code{summary}.}
}
\value{
\code{format} returns a character scalar; \code{print} returns its
  argument invisibly; \code{as.data.frame} returns a one-row data frame with
  the function, the enclosure, its decoration, the budget, whether the
  question was settled and the word; \code{summary} returns an object of class
  \code{ra_escalation_summary}, which prints the same fields with the
  argument box added.
}
\description{
Render an escalation for the console and for a data frame, with
  the word of its verdict and the budget it reached.
}
\details{
The word is printed always, including when the question was settled,
  so that a reader never has to infer from the presence of a number whether
  the number was reached or exhausted.
}
\section{Methodological notes}{

  These are written with the class rather than after it. A class whose
  printing and coercion arrive later spends the interval producing output that
  looks like a list of three fields, which is how an abstention gets read as a
  result.
}

\section{Dependencies}{

  Base R only.
}

\examples{
if (ra_has_mpfr()) {
  e <- ra_elem_escalate("sin", ra_interval(1, 1),
                        function(iv) ra_wid(iv) < 2^-52)
  print(e)
  as.data.frame(e)
  summary(e)
}
}
\references{
Wickham, H. (2019). Advanced R (2nd ed.). Chapman & Hall/CRC.
  https://doi.org/10.1201/9781351201315
}
\seealso{
[ra_elem_escalate()].
}
